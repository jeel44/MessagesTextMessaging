package text.message.sms.messaging.ui.theme

import androidx.compose.material3.Typography

/**
 * Material 3 defaults for now. The expressive type scale, including the display weights used
 * by the conversation list header, lands in a later pass.
 */
internal val MessagingTypography = Typography()
